#include <math.h>
#include <time.h>

float subtraction(float a, float b)
{

	return 0;
}

double fibonacci(int n)
{
	if(n < 2) return n;

	else return fibonacci(n-2)+fibonacci(n-1);
}

double min_arr(double *arr, int arr_length)
{
	double min;
	int i, arr_lenght;

	min = arr[0]
	for(i=0;i<arr_length;i++)
	{
		
	
}
